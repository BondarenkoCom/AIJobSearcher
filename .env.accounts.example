# Local-only secrets for browser/app accounts (do NOT commit).
# Copy to .env.accounts and fill values. This project loads .env and .env.accounts.
#
# Recommended approach: use Playwright "storage state" or a persistent profile
# (login once manually; scripts reuse the session) instead of storing passwords.

# --- Playwright settings ---
PLAYWRIGHT_HEADLESS=false
PLAYWRIGHT_SLOW_MO_MS=0
# Where Playwright keeps the logged-in browser profile (persistent context).
PLAYWRIGHT_USER_DATA_DIR=data/profiles/default
PLAYWRIGHT_USER_DATA_DIR_WORKANA=data/profiles/workana
PLAYWRIGHT_USER_DATA_DIR_FREELANCERMAP=data/profiles/freelancermap

# Optional: storage state files (if you use playwright.storage_state()).
LINKEDIN_STORAGE_STATE=data/profiles/linkedin_state.json
UPWORK_STORAGE_STATE=data/profiles/upwork_state.json
GLASSDOOR_STORAGE_STATE=data/profiles/glassdoor_state.json

# --- Accounts (NOT recommended; prefer storage state) ---
# Leave these empty if you can. Prefer logging in once manually and reusing the
# persistent profile / storage state. If you *must* use credentials here, keep
# them local (in .env.accounts), never commit.
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

UPWORK_EMAIL=
UPWORK_PASSWORD=

GLASSDOOR_EMAIL=
GLASSDOOR_PASSWORD=

FREELANCERMAP_EMAIL=
FREELANCERMAP_PASSWORD=

WORKANA_EMAIL=
WORKANA_PASSWORD=

# --- Optional LLM council for gig triage (local-only secrets) ---
OPENAI_API_KEY=
XAI_API_KEY=
COUNCIL_MODELS=openai/gpt-4.1,xai/grok-4-latest
CHAIRMAN_MODEL=openai/gpt-4.1

# --- Telethon (Telegram user account scan) ---
# Copy from your old Telethon project env if you already have a valid session.
TELETHON_API_ID=
TELETHON_API_HASH=
TELETHON_SESSION_STRING=
# Optional fallback if string session is empty.
TELETHON_SESSION_FILE=data/telegram/telethon_user.session
# Comma/newline-separated chat sources. Example:
# TELEGRAM_SOURCE_CHATS=@qa_jobs,@remoteitjobs,https://t.me/somechannel
TELEGRAM_SOURCE_CHATS=

# --- Optional proxy (if you use one) ---
HTTP_PROXY=
HTTPS_PROXY=


